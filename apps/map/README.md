# Fungi Interactive Map (reserved)

This workspace slot is reserved for the future interactive-map project. It shares the
pnpm workspace root and `packages/ui` design system with `apps/fungi`, but is not
scaffolded yet — it will be started as its own step when that project begins.
